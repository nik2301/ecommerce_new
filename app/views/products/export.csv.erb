<% headers = ['Name', 'Description', 'Price'] %>
<%= CSV.generate_line headers -%>
<% @products.each do |product| %>
  <%= CSV.generate_line([product.name, product.description, product.price]) -%>
<% end %>

<%# Using '-' to avoid line breaK -%>